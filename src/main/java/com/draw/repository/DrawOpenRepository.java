package com.draw.repository;

import com.draw.entity.DrawOpen;
import org.springframework.data.jpa.repository.JpaRepository;

public interface DrawOpenRepository extends JpaRepository<DrawOpen,Integer> {
}
